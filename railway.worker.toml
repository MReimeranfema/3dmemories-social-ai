[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A app.workers.celery_app worker --loglevel=info -Q celery,default,high,low"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
